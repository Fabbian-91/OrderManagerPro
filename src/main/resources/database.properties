# Database Configuration
# IMPORTANT: In production, use environment variables or secure configuration management
# Example: DB_URL=${DB_URL}, DB_USERNAME=${DB_USERNAME}, DB_PASSWORD=${DB_PASSWORD}
db.url=jdbc:postgresql://localhost:5432/ordermanager
db.username=postgres
db.password=postgres
